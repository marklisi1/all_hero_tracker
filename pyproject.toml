[project]
name = "all-hero-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "PyNaCl>=1.5",
    "python-dotenv>=1.0",
]
